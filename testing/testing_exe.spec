# -*- mode: python -*-
import sys  
sys.setrecursionlimit(10000)  

block_cipher = None

a = Analysis(['testing_exe.py'],
             pathex=['D:\\WorkStation\\Dropbox (UFL)\\WorkStation\\SRDense\\testing'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='testing_exe',
          debug=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='testing_exe')
